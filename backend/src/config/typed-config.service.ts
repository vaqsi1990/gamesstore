import { ConfigService } from '@nestjs/config';

/**
 * Type alias for ConfigService when using typed config (e.g. AuthConfig).
 * Use ConfigService from @nestjs/config; this is for type hints only.
 */
export type TypedConfigService = ConfigService;
